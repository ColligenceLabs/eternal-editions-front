import React from 'react';
import { Stack, Box } from '@mui/material';

const PhoneNumberChange = () => {
  return (
    <Stack>
      <Box sx={{ padding: '100px 50px', textAlign: 'center', fontSize: '32px', fontWeight: 700 }}>
        본인인증
      </Box>
    </Stack>
  );
};

export default PhoneNumberChange;
